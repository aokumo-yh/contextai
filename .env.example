# Context AI Configuration
# Copy this file to .env and add your API keys

# ============================================================================
# TRANSLATION PIPELINE FLOW:
# ============================================================================
# 1. User speaks → ElevenLabs STT or Web Speech API transcribes
# 2. Detect language automatically
# 3. Translate using DeepL (primary) or Google Translate (fallback)
# 4. Send BOTH original + translated to Claude Opus for analysis
# 5. Claude analyzes original text for cultural nuances
# 6. Display original, translation, and cultural insights to user
# 7. Speak insights via ElevenLabs TTS or Web Speech API
# ============================================================================

# 1. ElevenLabs API (Speech-to-Text & Text-to-Speech)
# ----------------------------------------------------------------------------
# Used for: Converting speech to text, generating natural voice output
# Get your key: https://elevenlabs.io/app/settings/api-keys
# Pricing: Free tier available, then $5/month for 30k characters
# Fallback: Web Speech API (browser built-in, free)
VITE_ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# 2. Anthropic Claude API (Cultural Analysis)
# ----------------------------------------------------------------------------
# Model: claude-opus-4-20250514
# Used for: Understanding cultural nuances, detecting soft no's, indirect communication
# Why Opus: Best at understanding subtle cultural context and business communication
# Get your key: https://console.anthropic.com/account/keys
# Pricing: $15 per 1M input tokens, $75 per 1M output tokens (~$0.015 per conversation)
# Free credits: $30 on sign up
# Fallback: Pattern-based analysis (less accurate)
VITE_ANTHROPIC_API_KEY=your_anthropic_api_key_here

# 3. DeepL Translation API (Primary Translator)
# ----------------------------------------------------------------------------
# Used for: High-quality, natural business translations
# Why DeepL: Most natural translations for business context
# Get your key: https://www.deepl.com/pro-api
# Pricing: Free tier 500k chars/month, then $5.49/month for 1M chars
# Fallback: Google Translate (if DeepL not configured)
VITE_DEEPL_API_KEY=your_deepl_api_key_here

# 4. Google Cloud Translation API (Fallback Translator)
# ----------------------------------------------------------------------------
# Used for: Translation when DeepL is unavailable, supports more languages
# Get your key: https://console.cloud.google.com/apis/credentials
# Pricing: $20 per 1M characters
# Fallback: No translation (shows original text only)
VITE_GOOGLE_TRANSLATE_API_KEY=your_google_translate_api_key_here

# ============================================================================
# DATABASE (Pre-configured)
# ============================================================================
# Supabase is already set up for conversation history and analytics
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Add your API keys above (at minimum, add VITE_ANTHROPIC_API_KEY)
# 3. Run: npm install
# 4. Start: npm run dev
# 5. The app will show which AI services are active in the sidebar
#
# MINIMUM REQUIRED: None! The app works with browser fallbacks.
# RECOMMENDED: VITE_ANTHROPIC_API_KEY for accurate cultural analysis
# OPTIMAL: All keys for full AI translation pipeline
# ============================================================================
